# Report Build Instructions

## Requirements

- TeX Live (or TinyTeX) with `latexmk` and `biber`

## Build

```bash
./build.sh          # Build PDF (output: build/main.pdf)
./build.sh watch    # Continuous build with auto-reload
./build.sh clean    # Remove build artifacts
```
